# Python
__pycache__/
*.py[cod]
*.egg-info/
.tox/
.venv/
venv/

# Ansible build artifacts
*.tar.gz
ansible_collections/
tests/output/
ee/context-out/
ee/context/
ee/akeyless-awx_integration.tar.gz

# Local Claude Code briefing file. Never commit.
CLAUDE.md

# Local working notes (commands that have been verified end-to-end).
# These get harvested into docs but never shipped raw.
notes/

# Throwaway secrets and cert material. Never commit.
test-fixtures/
*.key
*.pem
*.crt
*.csr
*.srl

# Embedded sample-project git directory used during local testing
tests/integration/project/.git/
tests/integration/project/collections/local/

# IDE / OS
.vscode/
.idea/
.DS_Store
